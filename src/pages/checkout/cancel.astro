---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Pago Cancelado - Benice" hideNewsletterPopup={true}>
  <div class="min-h-screen bg-gradient-to-b from-orange-50/50 via-white to-gray-50 py-10 sm:py-16">
    <div class="max-w-2xl mx-auto px-4 text-center">
      
      <!-- Icono -->
      <div class="mb-8">
        <div class="w-24 h-24 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-6 animate-pulse-slow">
          <svg class="w-12 h-12 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4.5c-.77-.833-2.694-.833-3.464 0L3.34 16.5c-.77.833.192 2.5 1.732 2.5z" />
          </svg>
        </div>
        
        <h1 class="text-3xl sm:text-4xl font-extrabold text-gray-900 mb-3">
          Pago no completado
        </h1>
        <p class="text-lg text-gray-500 max-w-md mx-auto">
          No te preocupes, tu carrito sigue intacto. Puedes intentar de nuevo cuando quieras.
        </p>
      </div>

      <!-- Info card -->
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 sm:p-8 mb-8 text-left">
        <h3 class="font-bold text-gray-800 mb-4 flex items-center gap-2">
          <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          ¿Por qué se canceló?
        </h3>
        <div class="space-y-3">
          <div class="flex items-start gap-3 p-3 bg-gray-50 rounded-xl">
            <span class="w-6 h-6 bg-gray-200 rounded-full flex items-center justify-center text-xs font-bold text-gray-600 shrink-0 mt-0.5">1</span>
            <div>
              <p class="text-sm font-medium text-gray-800">Cancelaste el proceso</p>
              <p class="text-xs text-gray-500">Pulsaste "Volver" o cerraste la ventana de pago de Stripe</p>
            </div>
          </div>
          <div class="flex items-start gap-3 p-3 bg-gray-50 rounded-xl">
            <span class="w-6 h-6 bg-gray-200 rounded-full flex items-center justify-center text-xs font-bold text-gray-600 shrink-0 mt-0.5">2</span>
            <div>
              <p class="text-sm font-medium text-gray-800">Problema con el pago</p>
              <p class="text-xs text-gray-500">La tarjeta fue rechazada o hubo un error en el procesamiento</p>
            </div>
          </div>
          <div class="flex items-start gap-3 p-3 bg-gray-50 rounded-xl">
            <span class="w-6 h-6 bg-gray-200 rounded-full flex items-center justify-center text-xs font-bold text-gray-600 shrink-0 mt-0.5">3</span>
            <div>
              <p class="text-sm font-medium text-gray-800">Sesión expirada</p>
              <p class="text-xs text-gray-500">Tardaste demasiado tiempo en completar el pago</p>
            </div>
          </div>
        </div>

        <div class="mt-5 p-3 bg-green-50 border border-green-200/60 rounded-xl">
          <p class="text-xs text-green-700 flex items-center gap-1.5">
            <svg class="w-4 h-4 text-green-500 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg>
            <strong>No se ha realizado ningún cargo.</strong> Tu dinero está seguro.
          </p>
        </div>
      </div>

      <!-- Botones -->
      <div class="flex flex-col sm:flex-row gap-3 justify-center">
        <a 
          href="/checkout" 
          class="px-8 py-4 bg-gradient-to-r from-purple-600 to-purple-700 text-white font-bold rounded-xl hover:from-orange-500 hover:to-orange-600 transition-all shadow-lg shadow-purple-200 hover:shadow-orange-200 flex items-center justify-center gap-2"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Intentar de nuevo
        </a>
        <a 
          href="/productos" 
          class="px-8 py-4 border-2 border-gray-200 text-gray-700 font-bold rounded-xl hover:border-purple-400 hover:text-purple-600 hover:bg-purple-50/50 transition-all flex items-center justify-center gap-2"
        >
          Seguir comprando
        </a>
      </div>

      <!-- Contacto -->
      <p class="mt-8 text-xs text-gray-400">
        ¿Necesitas ayuda? 
        <a href="/info/contacto" class="text-purple-600 hover:text-purple-700 font-medium underline">Contáctanos</a>
      </p>
    </div>
  </div>

  <style>
    @keyframes pulse-slow {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.05); opacity: 0.9; }
    }
    .animate-pulse-slow {
      animation: pulse-slow 2s ease-in-out infinite;
    }
  </style>
</Layout>
