---
/**
 * /admin/dashboard → redirige a /admin/
 * 
 * El dashboard principal está en /admin/ (index.astro) con AdminLayout.
 * Esta ruta se mantiene por compatibilidad con enlaces existentes.
 */
export const prerender = false;
return Astro.redirect('/admin/', 301);
---
